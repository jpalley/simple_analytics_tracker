<%= form_with(model: facebook_sync) do |form| %>
  <% if facebook_sync.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(facebook_sync.errors.count, "error") %> prohibited this facebook_sync from being saved:</h2>

      <ul>
        <% facebook_sync.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :table_name, style: "display: block" %>
    <%= form.text_field :table_name %>
  </div>

  <div>
    <%= form.label :event_name, style: "display: block" %>
    <%= form.text_field :event_name %>
  </div>

  <div>
    <%= form.label :event_value, style: "display: block" %>
    <%= form.text_field :event_value %>
  </div>

  <div>
    <%= form.label :last_counter, style: "display: block" %>
    <%= form.text_field :last_counter %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
